import { PrimaryKey } from "@mikro-orm/core";

export abstract class BaseEntity {

  /*
  @Property(type: DateTimeType)
  createdAt?: new Date();

  @Property({ 
  type: DateTimeType
  onUpdate: () => new Date(),
  })
  updatedAt?: Date

  */
}